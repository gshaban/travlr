CS 465 Module Four Test Notes
Student: Shaban Ghaith

Verification performed before packaging:

1. Validated JavaScript syntax:
   - app.js
   - app_server/models/db.js
   - app_server/models/travlr.js
   - app_server/models/seed.js
   - app_api/controllers/trips.js
   - app_api/routes/index.js
   - app_server/controllers/travel.js

2. Validated seed data:
   - data/trips.json parses successfully as JSON.
   - The file contains three Travlr trips: Gale Reef, Dawson's Reef, and Claire's Reef.

3. Tested database population:
   - Cleared the local travlr.trips collection.
   - Started the application with only npm start.
   - Confirmed the app seeded 3 trips into MongoDB through Mongoose.

4. Tested required routes:
   - http://localhost:3000/ returned HTTP 200.
   - http://localhost:3000/travel returned HTTP 200 and rendered trip data/images.
   - http://localhost:3000/api/trips returned HTTP 200 and returned 3 trip records in JSON format.

5. Confirmed dependency handling:
   - Mongoose is listed in package.json.
   - node_modules is included so the grader can run npm start without npm install.

AI Use Acknowledgment:
I used ChatGPT to help implement, review, and test the Module Four database, schema, API, and seed-data updates. I reviewed the final files and tested the application before submission.