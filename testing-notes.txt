CS 465 Module Seven Security Test Notes
Student: Shaban Ghaith

Verification performed before packaging:

1. Validated backend JavaScript syntax:
   - app.js
   - app_api/models/user.js
   - app_api/controllers/authentication.js
   - app_api/routes/index.js

2. Built the Angular admin SPA:
   - Ran npm run build from app_admin.
   - Angular build completed successfully.

3. Tested Express startup:
   - Ran npm start from the root travlr folder.
   - Confirmed the app loaded .env.
   - Confirmed MongoDB connected at mongodb://127.0.0.1/travlr.
   - Confirmed seed script skipped duplicate trip records safely.

4. Tested authentication API endpoints:
   - POST http://localhost:3000/api/register returned a JWT.
   - POST http://localhost:3000/api/login returned a JWT.

5. Tested protected API behavior:
   - PUT http://localhost:3000/api/trips/BAHA260810 without token returned HTTP 401.
   - PUT http://localhost:3000/api/trips/BAHA260810 with invalid token returned HTTP 401.
   - PUT http://localhost:3000/api/trips/BAHA260810 with valid token succeeded.
   - POST http://localhost:3000/api/trips with valid token succeeded.
   - DELETE http://localhost:3000/api/trips/:tripCode without token returned HTTP 401.
   - DELETE http://localhost:3000/api/trips/:tripCode with valid token succeeded.

6. Tested Angular front end:
   - Opened http://localhost:4200 while logged out.
   - Confirmed trip cards display and only Log In is visible.
   - Confirmed Add Trip, Edit Trip, and Delete are hidden while logged out.
   - Logged in through the Angular login form using the registered mock user.
   - Confirmed Log Out appears after login.
   - Confirmed Add Trip, Edit Trip, and Delete appear after login.
   - Opened Edit Trip and saved through the Angular UI.
   - Confirmed the protected Angular save returned to the trip listing successfully.
   - Logged out and confirmed admin CRUD buttons disappeared again.

AI Use Acknowledgment:
I used ChatGPT to help review the rubric, implement security, and test the project. I reviewed and tested the final files before submission.
